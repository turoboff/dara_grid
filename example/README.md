# data_grid example

This Flutter app runs the package showcase screen so the grid configuration and usage live next to the reusable package code.

Run it with:

```bash
flutter pub get
flutter run
```

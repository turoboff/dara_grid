name: data_grid
description: A reusable Flutter data grid with sorting, pagination, selection, persistence, and inline editing.
version: 0.1.1
publish_to: none

environment:
  sdk: ^3.11.4
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
